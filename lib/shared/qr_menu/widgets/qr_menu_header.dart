import 'package:flutter/material.dart';

/// Header widget for QR Menu
class QRMenuHeader extends StatelessWidget {
  const QRMenuHeader({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Extract header widget from UniversalQRMenuPage
    return const SizedBox.shrink();
  }
}
