import 'package:flutter/material.dart';

/// Search widget for QR Menu
class QRMenuSearch extends StatelessWidget {
  const QRMenuSearch({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Extract search widget from UniversalQRMenuPage
    return const SizedBox.shrink();
  }
}
