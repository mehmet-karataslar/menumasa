import 'package:flutter/material.dart';

class QRMenuProducts extends StatelessWidget {
  const QRMenuProducts({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Extract from UniversalQRMenuPage
    return const SizedBox.shrink();
  }
}
